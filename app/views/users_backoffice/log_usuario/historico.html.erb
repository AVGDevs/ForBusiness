<h1>Histórico</h1>

<table class="table table-striped table-hover table-responsive-md">
    <tr>
        <th>Email</th>
        <th>Ação</th>

<% @tarefa.each do |t| %>
    <tr>
        <td> <%= t.email %> </td>
        <td> <%= t.acao %> </td>
<% end %>
</table>

<%=link_to("Voltar", controller: 'fato', action: 'index', departamento_id: 1, status_id: 2 )%>
